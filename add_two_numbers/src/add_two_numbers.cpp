#include <add_two_numbers.h>

ListNode* Solution::addTwoNumbers(ListNode* l1, ListNode* l2) {
  auto copy_l1 = l1;
  auto copy_l2 = l2;

  while ((copy_l1->next != nullptr) || (copy_l2->next != nullptr)) {
  }
}